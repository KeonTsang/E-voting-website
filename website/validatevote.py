def validate(voter_id):
    
    # this function verifies that the voter is eligible to cast a vote
    # returns boolean value respective to whether they can vote

    # age verification

    # vote not already cast

    return
